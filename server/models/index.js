const User = require('./User');
const MenuItem = require('./MenuItem');
const Restaurant = require('./Restaurant');
const Order = require('./Order');

module.exports = { User, MenuItem, Restaurant, Order };
